Put your portrait here as `profile.jpg`.

- Portrait crop, roughly 4:5 (e.g. 800 x 1000 px) works best.
- It is used in two places: the hero arch on the home page, and the small
  circular avatar in the navbar.
- Until the file exists, both fall back to an "ET" monogram automatically.
